const Refresh = () => {//refers the words api to get new sets of words and then reset the states.
    return (
        <div
            className="cursor-pointer px-4 py-2 bg-green-500 w-fit text-white rounded hover:bg-green-600"
        >
            Refresh
        </div>
    );
};
export default Refresh;
